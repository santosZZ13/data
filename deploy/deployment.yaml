apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service-deployment
  labels:
    app: data-service-label-app

spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-service-label-app
  template:
    metadata:
      labels:
        app: data-service-label-app
    spec:
      containers:
        - name: data-service
          image: quangnam130520/data-service:latest
          ports:
            - containerPort: 8080
#          env:
#            - name: SERVER_PORT
#              value: "8080"
#            - name: SPRING_PROFILES_ACTIVE
#              value: "prod"
#          resources:
#            requests:
#              memory: "64Mi"
#              cpu: "250m"
#            limits:
#              memory: "128Mi"
#              cpu: "500m"
#          livenessProbe:
#            httpGet: # HTTP GET request
        - name: redis
          image: redis:7.4.0
          ports:
            - containerPort: 6379
#          resources:
#            requests:
#              memory: "64Mi"
#              cpu: "250m"
#            limits:
#              memory: "128Mi"
#              cpu: "500m"
#          livenessProbe:
#            exec: # Command
#              command:
#                - redis-cli
#                - ping
#          readinessProbe:
#            exec: # Command
#              command:
#                - redis-cli
#                - ping
